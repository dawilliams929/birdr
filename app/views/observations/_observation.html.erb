<h2>Observed by <%= link_to @observation.user.email, @observation.user %> on <%= @observation.date.to_formatted_s(:long_ordinal) %> at <%= link_to @observation.location.name, @observation.location %></h2>

<p>Number of species: <%= @observation.num_of_species %></p>
<div class="table-responsive">
	<table class="table table-striped table-condensed">  <tr>
	    <th>Species</th>
			<th>Count</th>
	  </tr>
	  <% @observation.sightings.each do |sighting| %>
	    <tr>
		  <td><%= link_to sighting.species.to_html, sighting.species %></td>
		  <td><%= sighting.count %></td>
		</tr>
	  <% end %>
	</table>
</div>