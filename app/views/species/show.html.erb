<%= render @species %>

<%= javascript_tag do %>
	var id = <%= params["id"] %>.toString();
<% end %>  <!-- this seems kinda weird to do -->

<style>
	html { height: 100% }
  body { height: 100%; margin: 0; padding: 0 }
	.container { height: 100% }
  #map-canvas { height: 100% }
</style>

<div id="map-canvas"></div>